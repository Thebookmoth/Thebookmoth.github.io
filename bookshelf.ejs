<%- include ('partials/head') %>

    <body>
        <%- include ('partials/navbar') %>
            <h1>Bookshelf Page</h1>
            <ul>
                <% for(let book of books){ %>
                    <li><a href="book/<%= book._id %> "> "<%= book.title %>" by <%= book.author %></a>
                    </li>
                    <% } %>
            </ul>

            <a href="/book/new"> Add a New Book</a>
            <%- include('partials/footer') %>